# interp-prep

Mechanistic interpretability reproductions on GPT-2 small (TransformerLens).
Pre-MSDS prep, summer 2026. Toward an interpretability capstone @ JHU AMS.

## Progress
- [x] Induction-head detection (`src/induction_heads.py`) — top heads L5H1, L6H9, L5H5 (score ~0.90)
- [ ] IOI circuit via activation patching (week 2-3)
- [ ] SAE feature inspection (week 4)

## Setup
```bash
python -m venv .venv
# Windows: .venv\Scripts\activate
pip install -r requirements.txt
python src/induction_heads.py
```
GPT-2 small runs on CPU. GPU optional.

## Results
Induction-head heatmap: `report_induction.png` (generated by the script).
